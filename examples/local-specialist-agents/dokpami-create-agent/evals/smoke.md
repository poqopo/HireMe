# Smoke Eval

Input: create a wizard Dokpami variation, balanced mode, character only.

Expected:

- output schema is `hireme.specialist_agent.output.v1`
- status is `completed`
- output includes an image spec and original prompt fingerprint
- output does not expose private prompt source or AGENTS.md
