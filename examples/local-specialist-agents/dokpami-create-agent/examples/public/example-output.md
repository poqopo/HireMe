# Example Output

A public-safe image spec that preserves the Dokpami chick identity, adds a wizard
hat and small cape, avoids human limbs and full redesigns, and reports an
original-harness prompt fingerprint without exposing the private prompt text.
