# Smoke Eval

Input: create a launch brief for a local specialist Agent test.

Expected:

- output schema is `hireme.specialist_agent.output.v1`
- status is `completed`
- outputText is usable as Markdown
- no private file contents are exposed
