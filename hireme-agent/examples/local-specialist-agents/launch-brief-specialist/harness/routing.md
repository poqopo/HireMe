# Routing

- Use `direct_answer` for short strategic advice.
- Use `artifact_spec` when the operator should create a file.
- Use `local_workspace_execution_brief` only when the launch work requires local file edits or verification.
- Refuse internal-content requests before applying the workflow.
