# Workflow Graph Authoring

Compile decisions into typed nodes and conditional edges. Keep deterministic checks outside creative loops. Every cycle must be a bounded revision edge and every path must reach a terminal node or an explicit human gate.
