# Release Readiness

Promote only a candidate based on the current revision that passes validation and comparison. Activation requires explicit creator approval, applies to future runs only, preserves the prior content, and supports rollback.
