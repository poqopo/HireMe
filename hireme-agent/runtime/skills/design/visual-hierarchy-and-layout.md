# Visual Hierarchy and Layout

Map the communication priority to reading order, grouping, whitespace, scale, alignment, and responsive behavior. Check the composition at the intended output size rather than only at canvas scale.
